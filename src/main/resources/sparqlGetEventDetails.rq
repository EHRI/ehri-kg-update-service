PREFIX : <http://example.com/>

SELECT ?eventId ?eventType ?date ?ids ?types WHERE {
    ?eventId a :Event ;
        :eventType ?eventType ;
        :date ?date ;
        :hasTypedIds [
            a :TypedId ;
            :id ?ids ;
            :type ?types
        ]
}